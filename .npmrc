# Project-level npm config — overrides any global ~/.npmrc
# Pin to the public registry so CI/CD runners without proxy or auth
# (e.g. GitHub Actions) can install dependencies reliably.
registry=https://registry.npmjs.org/
